# Email capture — set exactly one provider. Without either, /api/subscribe
# returns success in development (logged) and 503 in production.

# Substack publication URL (the embed-compatible free-signup endpoint is used)
SUBSTACK_URL=https://donefriday.substack.com

# …or a Buttondown API key
# BUTTONDOWN_API_KEY=
